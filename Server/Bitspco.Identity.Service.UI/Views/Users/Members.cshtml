<div id="members" class="modal modal-green">
    <div>
        <div class="header">
            <div class="title"><i class="mdi mdi mdi-file-tree"></i> <span class="px-10">لیست اعضاء</span></div>
            <div class="close"><a href="#">✖</a></div>
        </div>
        <div class="body container-xs">
            <form v-on:submit.prevent="add">
                <div class="flex">
                    <div class="flex-1">
                        <div class="form-group">
                            <label>کاربر : </label>
                            <select class="form-control" required v-model="model.MemberId">
                                <option v-bind:value="null">انتخاب کنید ...</option>
                                <option v-for="item in users" v-bind:value="item.Id" v-if="!items.find(x => x.MemberId == item.Id) && item.Id != id">
                                    {{item.Name}}
                                </option>
                            </select>
                        </div>
                    </div>
                    <div class="px-10">
                        <button class="btn btn-green-dark">افزودن</button>
                    </div>
                </div>
            </form>
            <table class="table striped bordered mt-10">
                <thead>
                    <tr>
                        <th style="width: 70px">شناسه</th>
                        <th>نام</th>
                        <th>نام کاربری</th>
                        <th>وضعیت</th>
                        <th style="width: 25px"></th>
                    </tr>
                </thead>
                <tbody>
                    <tr v-for="item in items">
                        <td>{{item.Member.Id}}</td>
                        <td>{{item.Member.Name}}</td>
                        <td>{{item.Member.Username}}</td>
                        <td>{{item.Member.GetStatusName()}}</td>
                        <td class="controls"><a class="btn btn-red" v-on:click="remove(item.MemberId)"><i class="mdi mdi-delete"></i></a></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>